# Production API origin (Render). Used by `next build` for Vercel deploys.
# Consumed by lib/api.ts (BASE_URL + API_ORIGIN) and the pre-hydration warm-up
# script in app/layout.tsx. .env.local overrides this for local development.
NEXT_PUBLIC_API_URL=https://crucible-backend-y1wm.onrender.com
